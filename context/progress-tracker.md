# Progress Tracker

> **Purpose**: Where the project is NOW. Update after every meaningful change.
> The three living files — `progress-tracker.md`, `flow.md`, `decision.md` — are read
> before ANY task.

## Current Phase

**Phase 4 — UI Improvements (IN PROGRESS)**

Player button layout redesigned to match Spotify/YouTube Music patterns: scrollable action pills row (mobile), static icon row (desktop), artwork tap zones for seek, reorganized control hierarchy.

## Current Goal

Frontend marketing/download site for Music Deewane — single-page app with animated navigation, GSAP animations, download buttons linking to GitHub releases, privacy policy, terms & conditions, SEO (robots.txt, sitemap.xml), deployed via GitHub Pages.

## Completed (Phase 3)

- **Frontend directory structure created** — `frontend/` with `css/`, `js/`, `assets/` subdirectories. Logo copied from `assets/logo.png`.
- **index.html** — Complete rebuild with Spotify-inspired layout: split hero (text left, mockup right), social proof strip, alternating zigzag feature rows (4 features, image/text alternating), stats band, download grid with platform strip, open source section, multi-tier footer (CTA banner, categorized link columns, bottom bar). SEO meta tags (Open Graph, Twitter Card, Schema.org). GSAP + ScrollTrigger loaded via CDN.
- **css/style.css** — Warm Teal theme (#0D9488 primary, #F8FAFC background), container-wise design, Inter font, responsive design (mobile-first with breakpoints at 480/768/1024px), all interactive states, prefers-reduced-motion support. New patterns: hero grid layout, social proof strip, feature-row zigzag grid, platform strip, footer columns grid.
- **js/main.js** — GSAP ScrollTrigger animations: hero entrance timeline (staggered badge, title, subtitle, CTA, mockup), social proof strip stagger, section header reveals, alternating feature row reveals (left/right based on row direction), stats counter, download cards stagger, platform strip stagger, open source and footer reveals. Header scroll, mobile hamburger, smooth scroll, active nav pill observer.
- **privacy.html** — Privacy policy page covering data collection, third-party services, open source disclaimer, developer no-liability clause.
- **terms.html** — Terms & conditions covering open source license, developer disclaimer, user responsibilities, third-party content, limitation of liability.
- **robots.txt** — Allows all crawlers, points to sitemap.xml.
- **sitemap.xml** — Lists all 3 pages with priorities and change frequencies.

## Not Started (explicitly)

- UI redesign (decided: full redesign — ADR-008)
- Feature modifications / custom functionality
- Dependency or provider changes (all current providers kept — ADR-007)
- Upstream synchronization (not scheduled — ADR-005)
- Community AI skills install (`python Skills.py -y`) + Spec Kit (`specify init`) — deferred (ADR-010)
- Platform work (all 6 platforms kept — ADR-009; iOS is currently unbuildable as committed — see open questions)

> Inspecting code is NOT completion — none of the above is done until built and approved.

## Next Up

1. Begin UI redesign via design-first workflow: spec → clarify → approve → implement (ADR-008).
2. Run `python Skills.py -y` + `specify init .` to install community skills and Spec Kit (ADR-010).
3. Decide remaining open questions that block future work.

## Open Questions

- **Repository initialized** — BloomeeTunes cloned as base; origin → `https://github.com/aditya452007/Music_Deewane.git`, branch `main`, pushed; `upstream` remote = BloomeeTunes. Single `.git`.
- **Template AI framework integrated** (commit `949c041`, 140 files) — `.agents/` skills, `context/`, `Agent.md`, `AGENTS.md`, `SKILLS.md`, `Skills.py`, `skills-lock.json`, `DESIGN*.md`, `States.md`.
- **Framework understood** — execution protocol (design-first, skill loading, context sync), skill index, Skills.py/community skills flow, Spec Kit (not yet installed).
- **Codebase discovery (8 parallel agents)** — architecture (Dart `lib/` + Rust `rust/src/`), UI layer, Android, iOS, desktop/web, Rust/plugin system, dependencies/external services, user journeys — all traced from code, unknowns flagged.
- **Context initialized** — `project-overview.md`, `architecture.md`, `ui-context.md`, `code-standards.md`, `flow.md` rewritten with real findings; `ai-workflow-rules.md` retained as-is (framework methodology).
- **Decisions recorded** — ADR-004 … ADR-012 in `context/decision.md` (fork setup, upstream strategy, rename deferral→completion, providers, UI direction, platforms, skills timing, framework adoption).
- **Upstream relationship documented** — divergence strategy, what changed / intentionally not changed, sync policy (see `project-overview.md`).
- **App rename (Bloomee → Music Deewane) complete** — `pubspec.yaml name: music_deewane`, 170+ Dart imports `package:music_deewane/`, symbols `MusicDeewanePlayer`/`MusicDeewanePlayerCubit`/`MusicDeewaneSwitch`/`MusicDeewaneDialog*`, Rust crate `rust_lib_music_deewane`, FRB regen, Windows/Linux/macOS/web binary/app names, CI artifacts `music_deewane_tunes_*`, exe `Music Deewane.exe`, About page credits (handle `@aditya452007`, 5 contact pills), README rewrite, 7 l10n ARBs regenerated. Verified: `flutter analyze` 0 errors, `dart format` clean, `cargo check` passes (2 warnings in generated code).
- **GitHub CI/CD Pipeline Fixed & Modularized** — Removed invalid `actions/checkout@v5` references (upgraded to `v4`), fixed Android split APK target names (`app-universal-release.apk`), and separated monolithic workflow into isolated platform files (`release-android.yml`, `release-windows.yml`, `release-linux.yml`). Recorded in ADR-013.
- **CI green (ADR-014)** — Bumped pinned Flutter to `3.44.9` everywhere (fixes Windows "Visual Studio 16 2019 not found" by supporting the VS 2026 generator), deleted the corrupt `libisar.so` wget step from `checkout.yml` (was blocking `Isar.initializeIsarCore(download: true)`), bumped `actions/checkout` → `v5` (Node 24), dropped inert `CMAKE_GENERATOR`. Found + fixed a latent migration bug: legacy downloads now map via `_stripYoutubePrefix(download.mediaId)` so `youtubeyt-1` → `yt-1` instead of being silently dropped. Verified: `flutter analyze` 0 errors, all 15 tests pass.
- **Android release packaging simplified (ADR-017)** — Changed CI to build only `flutter build apk --release` (universal APK only, no `--split-per-abi`), producing a single direct `.apk` file instead of 4 split ABIs + universal. Package `music_deewane_android_v{version}.apk`. Verified: CI generates one APK as advertised on the bloometune original release page.
- **Dependency fixed for the 3.44 SDK (ADR-016)** — Flutter 3.44.9 (ADR-014) made `IconData` `final`, which broke `icons_plus 5.0.0` at build time. Swapped to its maintained fork `iconsx_plus ^1.0.1` (same API/17 sets, verified publisher), renaming the import in 60 files. Verified: `flutter analyze` 0 errors, probe widget test compiles + renders icons.
- **Windows build fix (ADR-018)** — Fixed C2338 MSVC error from deprecated `/await` compiler option in `audio_service_win` and `permission_handler_windows` plugins. Added `_SILENCE_EXPERIMENTAL_COROUTINE_DEPRECATION_WARNINGS` compile definition in `windows/CMakeLists.txt` (CMake level, not post-build vcxproj patch). Verified: `flutter build windows --release` completes successfully.
- **Windows/Linux binary name fix (ADR-019)** — Changed `BINARY_NAME` from `"bloomee"` to `"Music Deewane"` in both `windows/CMakeLists.txt` and `linux/CMakeLists.txt`. Updated `Runner.rc` metadata (`InternalName`, `OriginalFilename`). Removed fragile post-build rename step from `release-windows.yml`. Builds now produce `Music Deewane.exe` / `Music Deewane` directly.
- **Dart analysis cleanup** — Fixed 23 issues across 8 files: `const_with_non_const` in `lyrics_state.dart`, `invalid_constant` in `search_suggestion_bloc.dart`, `CURRENT_MANIFEST_VERSION` → `currentManifestVersion` (camelCase) across 3 files, `unused_import` in `plugin_cache_store.dart`, `unnecessary_const` in `sentinel_values.dart`, `prefer_const_constructors` in `app_theme.dart` + `import_export_service.dart`, `use_build_context_synchronously` in `storage_setting.dart` + `playlist_screen.dart` + `local_music_screen.dart`. Verified: `flutter analyze` 0 issues.
- **Plugin bootstrap fixed (ADR-020)** — The `repositories.json` on the `ghpage` branch was UTF-16LE encoded and pointed to a dead `plugins/` URL returning `{"plugins": []}`. Fixed: replaced with clean UTF-8 JSON pointing to `Music_Deewane_factory` fork (`kojima-ui/bloom-factory` fork) at `https://github.com/aditya452007/Music_Deewane_factory/releases/latest/download/bex-factory.json`. Deleted unused `gh-pages` branch. Factory repo has 12 working plugins (ytmusic, jiosaavn, lrcnet, billboard, spotify/ytmusic/jiosaavn/apple-music importers, search suggestion providers). CI workflow (`bex-factory.yml`) builds `.bex` files from source and publishes GitHub Releases.
- **Hover effects for desktop UI (ADR-021)** — Added subtle background tint (white 5% opacity) + hand cursor (`SystemMouseCursors.click`) on all clickable elements for desktop/web. Applied to: SongCardWidget, _ActionButton, SquareImgCard, CarouselCardView, ChartListTile, LibItemCard, SettingTile, HorizontalNavBar (GNav), VerticalNavBar (NavigationRail), LikeBtnWidget, PlayPauseButton. Created reusable `HoverWrapper` widget (available for future use). Files modified: `song_tile.dart`, `square_card.dart`, `carousel_card_widget.dart`, `chart_list_tile.dart`, `libitem_tile.dart`, `setting_tile.dart`, `global_footer.dart`, `like_widget.dart`, `play_pause_widget.dart`, `hover_wrapper.dart` (new). Verified: `dart format` clean on all files.
- **Top Picks section (YouTube Music Speed Dial style)** — Replaced "Recently Played" (`TabSongListWidget`) on Explore screen with paginated `TopPicksWidget`. 3×3 grid on mobile, 4×2 on tablet/desktop via `PageView` + `GridView.count`. Square cards with gradient scrim (bottom 40%), title/artist overlay, press (scale 0.96 + opacity 0.85) and hover (white 5% tint + hand cursor) effects matching existing `SquareImgCard` pattern. Page dots with tap navigation, refresh button re-shuffles track order. Data from existing `RecentlyCubit` (shuffled client-side). Files: `top_picks_widget.dart` (new), `explore_screen.dart` (replaced 64 lines with 1 line).
- **App-wide hover effects & hand cursor** — Extended `Hoverable` widget (cursor + hover overlay, no GestureDetector) to `hover_wrapper.dart`. Applied across 11 files (~50 widgets): explore screen nav icons, mini player controls, player screen AppBar + control row + shuffle/external link, library screen actions, local music screen actions, offline screen actions, carousel item tap, horizontal card scroll buttons, top picks refresh + page dots, tab list scroll buttons. Verified: `flutter analyze` 0 errors, 0 warnings.
- **Void Monochrome redesign — player & theme (ADR-022)** — Replaced the red/pink (`#FE385E`) + cyan (`#0EA5E0`) color system with a pure monochrome palette: `#09090B` neutral-black background, `#18181B`/`#27272A` surfaces, `#FAFAFA` foreground, white accents, black-on-white high-contrast playback controls. New organic scalloped/flower-petal control language (`OrganicScallopShape`, `OrganicIconButton`, `play_pause_widget.dart` — white flower button, black icon, ~900 ms white "relief burst" particle animation on play). New `WaveProgressBar` — the played portion flows as an animated sine wave (freezes on pause), flat white, no glow. Removed ambient artwork glow (`AmbientImgShadowWidget`, `pallete_generator` usage in player_screen), killed the red/blue play/pause color shift, minimized mini-player progress bar glow, simplified `LikeBtnWidget` (white fill / gray outline). Fixed all white-on-white buttons app-wide (FilledButton/ElevatedButton theme defaults + onboarding, local music, timer, import/export, plugin manager, playlist, import process/media). Deleted dead `gradient_progress_bar.dart`. Verified: `flutter analyze` 0 errors, 0 warnings.
- **Flutter 3.44.9 / Dart 3.12.2 build fix (ADR-024)** — Resolved all build errors after SDK upgrade. Key changes: (1) `file_picker` 6.2.1→12.0.2: replaced 8 `FilePicker.platform.*` calls with static methods in `import_export_service.dart`, `playlist_screen.dart`, `local_music_screen.dart`, `plugin_manager.dart`, `storage_setting.dart`. (2) `ffi` override `^1.1.2`→`^2.2.0` for `nativeFree` compatibility. (3) Isar `.g.dart` regenerated via `dart run build_runner build`. (4) `permission_handler` downgraded `^13.0.1`→`^12.0.3` (SDK 37 doesn't exist as stable; 12.x uses compileSdk 35). (5) `compileSdk = 36`. (6) Gradle `8.12.0`→`8.14.0`, AGP `8.9.1`→`8.11.1`, Kotlin `2.1.0`→`2.2.20`. (7) Fixed all 51 analyzer info warnings across the codebase.
- **Android applicationId renamed (ADR-024)** — Changed `ls.bloomee.musicplayer`→`com.music_deewane` in `build.gradle.kts`, `MainActivity.kt` (moved to `com/music_deewane/`), `share_targets.xml`, Linux `CMakeLists.txt`, l10n ARB backup paths. Existing installs won't upgrade-in-place (intentional for rebrand).
- **CI Android build unblocked (ADR-024)** — Removed `android-actions/setup-android@v3` SDK 37 install step from `release-android.yml` (no longer needed after permission_handler downgrade). CI now uses standard Flutter-provided Android SDK with compileSdk 36.
- **Player UX refined (ADR-025)** — Controls follow Spotify/YouTube Music standard layout. Mobile (≤tablet): Line 1 = Shuffle | Prev | Play | Next | Repeat; Line 2 = Lyrics. Desktop (>tablet): Line 1 = Shuffle | -10s | Prev | Play | Next | +10s | Repeat | Volume slider. ±10s visible on desktop only. Fixed volume slider race condition (`_isDragging` set before `_setVolume()`). Inline lyrics auto-scrolls to center via `ScrollablePositionedList` (alignment 0.4). Fixed loop popup: removed `onPressed: () {}` from OrganicIconButton child of PopupMenuButton. OrganicIconButton only creates InkWell when onPressed != null. Responsive spacing (20 mobile / 32 desktop). Verified: `flutter analyze` 0 errors.
- **Home section redesign (ADR-026)** — Restructured Explore screen to match Spotify/YouTube Music layout patterns: (1) Added `QuickAccessChips` — horizontal scrollable pill row showing user playlists (pinned first, then unpinned) with cover art thumbnails + rounded pill styling. Uses `LibraryItemsCubit`. (2) Converted `TopPicksWidget` from paginated grid (3×3/4×2 + PageView + dots) to horizontal scroll list (150px cards, horizontal ListView). Removed PageController, dots, shuffle reset. Added desktop arrow navigation (reuse Hoverable pattern). (3) Moved chart carousel from position 2 to position 4 (after TopPicks + Last.fm). New section order: DiscoverBar → QuickAccessChips → TopPicks → Last.fm → Charts → Plugin sections. Verified: `flutter analyze` 0 issues.
- **UI polish (ADR-027)** — Multiple UI improvements: (1) TopPicksWidget reverted to grid layout (3×3 mobile, 5×4 desktop) with PageView pagination. (2) Lyrics view now has glass morphism background (backdrop blur + translucent surface + border). (3) ClearQueueButton in UpNextPanel now has hover effects. (4) Removed unused CarouselCardView widget. (5) Added RecentlyPlayedSection to Explore screen showing listening history. (6) Integrated new logo.jpeg, replaced bloomee_new_logo_c.png references. (7) Increased player button spacing for better touch targets (16→20px between main controls, 12→16px for secondary).
- **UX error handling & empty states (ADR-028)** — Comprehensive UX audit against States.md checklist. 6 parallel agents implemented: (1) Search: cross-source fallback (YouTube Music/JioSaavn/YouTube Video links on empty results), skeleton loading, offline hint. (2) Playback: first-retry toast, failure message with source alternatives, queue-finished prompt. (3) Network: persistent `OfflineBanner` widget in `global_footer.dart` with auto-dismiss "back online" snackbar. (4) Library/Downloads: guided empty states with action buttons (Search/Import/Scan/Browse). (5) Lyrics: not-found view with manual search + plugin buttons, timeout feedback (8s/20s progressive messages), import error specificity. (6) Plugins: health status dot (green/yellow), install retry snackbar, all-failed dialog (once per session). Fixed `use_build_context_synchronously` in `add_to_playlist_screen.dart`. Verified: `flutter analyze` 0 issues. ~450 lines added across 12 files, ~60 ARB keys across 7 locales. New file: `offline_banner.dart` (40 lines).
- **Lyrics/carousel/search fixes (ADR-029)** — User feedback: (1) Removed glass morphism container (Container/ClipRRect/BackdropFilter) from lyrics view — lyrics now serve directly in the artwork space like Spotify/YouTube Music (tap artwork → lyrics replace it). Removed unused `dart:ui` import. (2) Removed `CaraouselWidget` entirely from Explore screen (was overriding TopPicks). (3) Removed `RecentlyPlayedSection` and `_RecentlyPlayedCard` classes (~140 lines) — redundant with TopPicks. Removed unused `recently_cubit.dart` and `load_image.dart` imports. (4) Replaced external `_SourceLink` buttons (url_launcher) with in-app `ActionChip` source-switching chips — tapping a chip switches the active content plugin and re-runs the search. Removed `_SourceLink` class and `url_launcher` import. Verified: `dart analyze lib/` 0 issues.
- **Smart recommendations & onboarding wizard (ADR-030)** — Multi-step onboarding: Step 1 (Language + Country, existing UI), Step 2 (Artist Selection — search via ContentBloc plugin, select 3-5 favorites, circular cards with checkmark, skip if plugins not loaded), Step 3 (Music Language Preference — multi-select 18 language chips). Settings: `musicLanguages` (JSON list) + `favoriteArtists` (JSON list of `{id, name}`) added to SettingKeys + SettingsState + SettingsCubit. RecommendationCubit: exponential decay recency scoring (e^(-days/7)), artist frequency weighting, top-10 artist boost (1.5x), "For You" horizontal card list on Explore screen with "Because you listened to..." context. ManagePreferencesScreen in Settings for post-onboarding updates. ~12 new l10n ARB keys. Verified: `dart analyze` 0 issues on all new/modified files.
- **UX fixes & sidebar redesign (ADR-032)** — 6 files modified, ~500 lines changed. (1) Onboarding reordered: Language/Country → Music Language → Artist Selection. Skip button added (bypasses all remaining steps). BlocListener on _StepArtists for late-loading plugins. (2) Desktop sidebar redesigned: Spotify-style collapsible sidebar (72px collapsed icons-only, 280px expanded with playlist list). Uses LibraryItemsCubit for playlist items. Toggle button, nav items (Home/Search), "Your Library" header, scrollable playlist list with cover art. (3) For You section redesigned: tracks grouped by artist reason ("Because you listened to [Artist]"), each group has paginated grid (3×3 mobile, 5×4 desktop) matching TopPicksWidget pattern. (4) TopPicksWidget fixes: refresh button now shuffles tracks client-side, page dots track actual PageController position. (5) RecommendationCubit: fixed applyFavoriteArtists bug (daysAgo always 0), now delegates to full recomputation. (6) Explore screen: added SearchIcon to CustomDiscoverBar. (7) New l10n key: `onboardingSkip`. Verified: `dart analyze` 0 issues, `dart format` clean on all files.
- **UX refinements (ADR-033)** — 4 UX improvements across 7 files. (1) ForYou section simplified to single top-artist: cubit picks most-played artist, filters tracks, adds `topArtistName` to state. Widget renders one "Because you listened to {artist}" group instead of multiple. (2) Default artists in onboarding: new `default_artists.dart` file with language/country-based artist suggestions (12 per locale, 7 locales + fallback). Shown before search in `_StepArtists` and in `_buildNoPluginsHint`. (3) Player overlay respects sidebar: `SidebarWidthNotifier` InheritedWidget provides sidebar width to player overlay. On desktop, player gets `Padding(left: sidebarWidth)`. GlobalFooter tracks sidebar expanded state. (4) Library header tappable: sidebar "Your Library" header navigates to Library tab on tap. (5) Build fix: `navigationShell` → `widget.navigationShell` in `_GlobalFooterState._handleHardwareBackPress` (forgotten when converting StatelessWidget → StatefulWidget). (6) Removed unused `appui_setting.dart` import in `setting_view.dart`. Verified: `dart analyze` 0 issues.
- **Sidebar & player fixes (ADR-034)** — 3 files modified. (1) Mini player bleed fixed: moved MiniPlayerWidget from `bottomNavigationBar` into content area Column (both mobile and desktop). On desktop, mini player sits inside the Expanded content area, no longer bleeds into sidebar. (2) Sidebar nav items expanded: added Local (index 3) and Offline (index 4) as separate nav items in `_SpotifySidebar` with their own icons (MingCute.music_2_fill, MingCute.folder_download_fill) and l10n labels. Collapsed sidebar now shows 5 icons: Home, Search, Local, Offline, Library. (3) Mini player removed from sidebar collapsed view — sidebar no longer shows MiniPlayerWidget when collapsed. (4) Full player two modes: new `PlayerExpandNotifier` InheritedWidget in `player_overlay_wrapper.dart`. Default mode respects sidebar width (existing behavior). Expand button in player AppBar toggles fullscreen — when expanded, player covers everything including sidebar (padding left = 0). Expand/exit icons from MingCute (fullscreen_line/fullscreen_exit_line). Verified: all icons confirmed in iconsx_plus, l10n keys verified in all 7 locales.
- **Desktop player pills unification (ADR-036)** — 1 file modified, ~100 lines deleted. (1) Replaced `_ActionIconRow` (OrganicIconButton bloom icons, 4px spacing) with `_ActionPillsRow` for desktop — same pill-style buttons as mobile with `isDesktop` flag for larger spacing (12px), icon size (20px), text (14px), height (44px). (2) Fixed download button: removed `Navigator.pop(context)` that was closing the player and causing blank page. (3) Deleted unused `_ActionIconRow` and `_ActionIconBtn` classes. Verified: `dart analyze` 0 issues.
- **Onboarding trim + changelog/about removal (ADR-037)** — 3 files deleted (~1,580 lines), 10 modified. (1) Onboarding reduced from 3 steps to 1: removed Music Language step + Artist Selection step (and `default_artists.dart`); kept single Language + Country step with Skip/Continue both persisting settings and marking onboarding done. (2) "What's New" changelog removed: deleted `changelog_reader.dart`, removed `WhatIsNewState` + its emit/listener, removed `fetchChangelog()` + `readChangelogs` setting key; update-available dialog kept. (3) About/developer page deleted (`about.dart`), along with its Settings "Info" group and the `SiteIcon` in the discover bar. Intentionally kept: `OnboardingService` gate, `musicLanguages`/`favoriteArtists` setting keys, `ManagePreferencesScreen`, `RecommendationCubit`/For You section. Verified: scoped `dart analyze` + `dart format` clean on all touched files; full `flutter analyze` 0 issues.
- **Git history reset (ADR-040)** — Full mirror backup created and verified first. Both `main` and `ghpage` rewritten as orphan branches with a single commit under aditya452007's identity; force-pushed with `--force-with-lease`. GitHub now shows sole contributor, one commit per branch. Release tags `v1.0.1*` deleted (v1.0.0+x kept). Old history recoverable only from the mirror backup. Supersedes ADR-004's keep-fork-history decision.
- **Search chip contrast fix completed (ADR-039)** — ADR-038 missed the selected chip: white fill had near-white text (`primaryColor2`), making the active filter invisible. Fixed: selected text is now black (`accentColor2dark`, w800); unselected chips pushed to fill α0.12, border α0.28, fully opaque white labels. Source chips labels also fully opaque. Verified: `dart format` + scoped `dart analyze` 0 issues.
- **Search chips contrast + selection highlight + Top Picks clipping (ADR-038)** — 3 files modified. (1) Search screen chips made legible: unselected category chips fill α0.08→0.10, border α0.05→0.18, label α0.8→0.92; unselected source chips border α0.15→0.35, label α0.8→0.92. (2) Text-selection highlight changed from solid white (hid selected words) to white at 30% opacity in `AppTheme.defaultThemeData`. (3) Top Picks bottom row no longer clipped: grid container height now computed via LayoutBuilder from actual width (`rows × tileWidth/0.9 + spacing`) instead of hard-coded `rows × 110px`. Verified: `dart format` + scoped `dart analyze` 0 issues.
- **Windows MSIX + Inno Setup installer (ADR-041)** — Added MSIX packaging and Inno Setup installer to Windows CI/CD pipeline. (1) Added `msix:` configuration block to `pubspec.yaml` (publisher: Music Deewane, capabilities: internetClient/musicLibrary, logo: assets/icons/logo.png). (2) Created `installer/windows/music_deewane.iss` Inno Setup script with desktop shortcut, Start Menu shortcut, launch after install, custom install location, uninstaller. (3) Updated `release-windows.yml`: added MSIX creation step (`flutter pub run msix:create`), Inno Setup installation via winget, ISCC compilation step, upload all three artifacts (zip + msix + exe) to GitHub releases. No Flutter/Dart code changes — only pubspec config + CI workflow + new .iss file.
- **In-app update from GitHub releases (ADR-042)** — Replaced SourceForge browser redirect with in-app download + platform install. (1) Created `update_service.dart` (~70 lines): HTTP streaming download with progress, platform-specific install (OpenFilex for Android APK, Process.run for Windows/Linux). (2) Fixed `GlobalEventsCubit` to pass `updates["download_url"]` instead of hardcoded SourceForge URL. (3) Updated `GlobalEventListener` update dialog: "Update Now" triggers download progress dialog → auto-install on complete. (4) Updated `CheckUpdateView` settings screen: same download flow. (5) Added `open_filex: ^4.7.0` dependency. Verified: `dart analyze` 0 issues, `dart format` clean.
- **Plugin ID rebranding (ADR-043)** — Replaced all `bloomfactory` plugin IDs with `musicdeewanefactory` across both repos. Music_Deewane_factory: all 12 manifest.json files (id + publisher.name), GitHub workflow TAG prefix + factory name. Music_Deewane: 3 Dart constants in `legacy_media_id_mapper.dart`, 3 in `legacy_migration_service.dart`, 5 test assertions in `legacy_migration_service_test.dart`. Zero `bloomfactory` references remain. Song info screen now shows `musicdeewanefactory` instead of `bloomfactory`.
- **Updater bug fixes (ADR-044)** — 6 bugs fixed across 8 files. (1) Removed `parseBuild` modulo 1000 logic that caused comparison failures at the 1000 boundary (build 1000 vs 1001 → modulo 1 → false negative). (2) Removed 🌸 cherry blossom emoji from all 7 locale ARB files and generated localization files (updateUpToDate, updateNewVersionAvailable, updateAvailableBody). (3) Removed independent `getLatestVersion()` call from `NotificationCubit` — it now only loads persisted notifications, eliminating duplicate update HTTP requests and inconsistent state between `GlobalEventsCubit` and `NotificationCubit`. (4) Fixed hardcoded SourceForge download URL in `GlobalEventsCubit` — now passes the actual `download_url` from the API response (GitHub asset URL or SourceForge URL). (5) Added Linux support to `extractUpUrl` — was previously only matching `windows` and `android` in asset URLs. (6) Fixed changelog "What's New" flow — now compares version AND build equality (`currFull == newFull`) instead of version only, preventing the changelog screen from showing when there's a genuine update available. Verified: `dart format` clean on all modified files.

## Not Started (explicitly)

- UI redesign (decided: full redesign — ADR-008)
- Feature modifications / custom functionality
- Dependency or provider changes (all current providers kept — ADR-007)
- Upstream synchronization (not scheduled — ADR-005)
- Community AI skills install (`python Skills.py`) + Spec Kit (`specify init`) — deferred (ADR-010)
- Platform work (all 6 platforms kept — ADR-009; iOS is currently unbuildable as committed — see open questions)

> Inspecting code is NOT completion — none of the above is done until built and approved.

## Next Up

1. Begin UI redesign via design-first workflow: spec → clarify → approve → implement (ADR-008).
2. Run `python Skills.py -y` + `specify init .` to install community skills and Spec Kit (ADR-010).
3. Decide remaining open questions that block future work.

## Open Questions

Unknowns from discovery — need developer confirmation (all in `context/architecture.md` too):

1. ~~Default plugin catalogue content (`repositories.json`) — which plugins ship by default?~~ **RESOLVED (ADR-020):** Points to `Music_Deewane_factory` fork with 12 plugins (bloom-factory).
2. Plugin SDK / canonical `plugin.wit` — where do plugins come from / how are they built?
3. iOS: unbuildable as committed (no Podfile, no `UIBackgroundModes`, no permission strings) — is iOS in scope for a future build?
4. Android `versionCode = 0` — fix before any store release?
5. Android release signing — no keystore committed (falls back to debug signing); who owns the release key?
6. macOS Release entitlements lack `network.client` — does streaming work in sandboxed release?
7. Web (wasm32) Rust build not wired — is the web target maintained?
8. `quinn` in Cargo.lock (not in Cargo.toml) — needed?
9. Android `network_cfg.xml` orphaned; `share_targets.xml` → `MainActivity` vs launcher `AudioServiceActivity` — intentional?
10. OS notification-permission flow — where is it handled?
11. `offline_views/downloads_status.dart` — dead code?
12. Unregistered route constants (`MainPage`/`TestPage`/`MusicPlayer`) — vestigial?
13. Last.fm scrobbling — actively maintained or legacy?
14. Target audience/product intent for Music_Deewane beyond "fork" (branding direction).

## Architecture Decisions

See `context/decision.md` — ADR-004 … ADR-042 (project decisions), ADR-001 … ADR-003 (template history).
